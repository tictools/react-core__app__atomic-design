# core

- `/Book
- --`/types` (as domain layer)
- --`/mappers` (as application layer)
- --`/queries` (as infra layer)

# services

- --`/http/request`
  - implement in queries

# app

- implement `core`
